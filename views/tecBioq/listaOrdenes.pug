.container.divLista
 ul.list-group.list-group-flush
             each o in ordenes
               li.list-group-item.list-group-item-action
                 div
                  p 
                   | #{o.id || ''} #{o.Paciente.Usuario.nombre || ''} #{o.Paciente.Usuario.apellido || ''}
                  each e in o.Examens 
                   p  
                    a(class="link-offset-2 link-underline link-underline-opacity-0" href=`http://localhost:3000/tecBioq/ordenExamenes/${o.id}/${e.id}`)= `${e.nombre} (${e.Muestra.nombre})`    
             - for(let i=ordenes.length; i<limit;i++)
               li.list-group-item.list-group-item-action &nbsp;
             if totalRegistros
               include ../paginado.pug
               +paginado('http://localhost:3000/tecBioq/ordenes')