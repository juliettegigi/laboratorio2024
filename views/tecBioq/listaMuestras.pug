.container.divLista
 h1.form-div Muestras
 ul.list-group.list-group-flush
             each m in muestras
               li.list-group-item.list-group-item-action
                 a(class="link-offset-2 link-underline link-underline-opacity-0" href=`http://localhost:3000/tecBioq/muestra/${m.id}`)
                     | #{m.id || ''} #{m.nombre || ''}
             - for(let i=muestras.length; i<limit;i++)
               li.list-group-item.list-group-item-action &nbsp;
             if totalRegistros
               include ../paginado.pug
               +paginado('http://localhost:3000/tecBioq/muestras')